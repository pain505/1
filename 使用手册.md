# 取件码管家 · 使用手册

> 这份是给**你自己**看的操作说明。技术细节和编译方法在 `README.md` / `BUILD.md`。

---

## 一、装好之后第一件事

打开 App，你会看到三块提示。**务必都处理掉**，否则它拿不到取件码：

| 提示 | 点它会发生什么 | 为什么必须做 |
|---|---|---|
| ① 允许读取短信 | 弹出系统权限框，点「允许」 | 快递短信全靠这个权限。不给就永远收不到 |
| ② 开启通知使用权 | 跳到系统的「通知使用权 / 通知读取权限」列表 | 拼多多、淘宝、京东、菜鸟的取件码都在通知里 |
| ③ 自动导入历史短信 | 无需操作，授权完成后自动跑 | 把最近 60 天的取件码一次补齐 |

**②的路径每个品牌不一样，找不到就在这里搜：**

- 小米 / Redmi（HyperOS、MIUI）：设置 → 应用设置 → 应用管理 → 取件码管家 → 权限管理 → 通知使用权（或：设置 → 隐私保护 → 特殊权限设置 → 通知使用权）
- 华为 / 荣耀（HarmonyOS、EMUI）：设置 → 应用 → 应用管理 → 取件码管家 → 权限 → 通知使用权
- OPPO / 一加（ColorOS）：设置 → 应用 → 特殊应用权限 → 通知使用权
- vivo / iQOO（OriginOS）：设置 → 应用与权限 → 权限管理 → 权限 → 通知使用权
- 三星（One UI）：设置 → 通知 → 更多设置 → 通知使用权

在列表里找到「取件码管家」，把开关打开。打开后回到 App，列表会自动刷新。

> 顺带把省电策略也放开：设置 → 应用 → 取件码管家 → 电池 → 选「不受限制 / 允许后台活动」。
> 部分系统（尤其小米、OPPO）会杀掉后台 App，导致短信广播收不到。

---

## 二、日常怎么用

### 界面长什么样

```
┌────────────────────────────────────────┐
│  取件码管家            [⋮ 菜单]         │
│  未取 3 件 · 今日到期 1 件               │
├────────────────────────────────────────┤
│  ▸ 菜鸟驿站(阳光花园店)                  │  ← 按取件点分组
│  ┌──────────────────────────────────┐  │
│  │  8-3-2015          还剩 5 小时    │  │  ← 码大号加粗，右侧倒计时
│  │  菜鸟驿站 · 2小时前                │  │
│  └──────────────────────────────────┘  │
│  ┌──────────────────────────────────┐  │
│  │  8-3-2016          今天 21:00 前  │  │
│  │  菜鸟驿站 · 2小时前                │  │
│  └──────────────────────────────────┘  │
│                                        │
│  ▸ 丰巢智能柜(A区)                       │
│  ┌──────────────────────────────────┐  │
│  │  470812            已过期 2 天     │  │
│  │  丰巢 · 3天前                      │  │
│  └──────────────────────────────────┘  │
│                                        │
│  ▸ 已取件                              │  ← 已取件自动折叠到最底
└────────────────────────────────────────┘
```

### 去取件的三步

1. **点一下那一行** → 取件码自动复制到剪贴板，到驿站直接粘贴给店员看。
2. 取完回来 **长按那一行** → 选「标记已取」 → 它变灰并折叠到最底部的「已取件」分组。
3. 攒多了点菜单 →「清空已取件」，一次清干净。

### 菜单里的四个功能

| 菜单项 | 什么时候用 |
|---|---|
| **立即扫描短信** | 刚给了权限、或发现漏了件。会重扫最近 60 天短信 |
| **粘贴导入** | 微信/支付宝小程序里的取件码：先长按复制那段文字，再来点这个 |
| **清空已取件** | 清理已取的记录 |
| **使用说明** | 就是这份文档的摘要 |

### 长按菜单的其他选项

- **标记未取** —— 手滑标错了可以撤回来
- **复制原文** —— 复制整条短信原文
- **查看原文** —— 看看这条取件码是从哪条短信/通知里来的
- **删除** —— 彻底删掉这条记录

---

## 三、微信 / 支付宝小程序的取件码怎么办

微信和支付宝**不给第三方 App 读消息的接口**，所以自动抓不到。两条路：

**方法一：分享（最快）**
在微信里长按那条取件码消息 → `转发` / `更多` → 找「取件码管家」→ 点一下，自动导入。

**方法二：复制 + 粘贴导入**
长按文字 → `复制` → 打开取件码管家 → 菜单 →「粘贴导入」。

两种都是一次点两下的事，比记取件码可靠。

---

## 四、连包裹状态都不用记：它会自己来

配置好之后，正常流程是**完全无感**的：

```
快递到站 → 驿站发短信 / App 推通知
              ↓（系统广播 / 通知回调，App 不需要打开）
       App 自动解析出取件码 + 网点名 + 过期时间
              ↓
        列表里多一行，按取件点分组
              ↓（App 不在前台时）
        系统通知栏提醒你「新取件码 8-3-2015」
```

你唯一需要做的就是**想起来的时候打开看一眼**。

---

## 五、疑难排查

**Q：短信来了但列表里没有？**
1. 先确认「读取短信」权限是开的（打开 App 看顶部还有没有提示条）。
2. 再确认省电策略没杀后台（见第一节末尾）。
3. 手动点菜单 →「立即扫描短信」补一次。
4. 还是没有？把那条短信原文**长按复制**，菜单 →「粘贴导入」试试。
   - 如果粘贴导入也没有 → 是解析规则漏了这种格式，值得反馈补规则。
   - 如果粘贴导入有、短信没进来 → 是权限/后台被杀的问题。

**Q：码显示成「未解析出码」（灰色）？**
说明这条短信里有「取件码」之类的关键词，但没提取出具体号码。
长按那行 →「查看原文」，把原文发出来补规则即可。**这类条目不会静默消失，是故意留给你看的。**

**Q：同一件包裹出现了两行？**
一般不会（同一条短信重复收到时会自动合并）。如果出现了，长按删掉一行即可。

**Q：「还剩多久过期」显示「约 3 天」是什么意思？**
短信里没写明确时间时，按到件时间 + 3 天估算，前面带「约」字表示是估算值。
驿站一般免费保管 3~5 天，快递柜通常 24 小时内免费。

**Q：我换成 iPhone 了还能用吗？**
不能。iOS 不允许第三方 App 读短信。iPhone 上只能靠「快捷指令」做短信自动转发，
或者手动复制粘贴到备忘录。

---

## 六、隐私

- App **没有申请 `INTERNET` 权限** —— 它物理上无法联网。你可以打开 `AndroidManifest.xml` 核对。
- 所有解析、所有数据都在你手机本地的 `SharedPreferences` 里。
- 唯一的外部影响面是：解析用的原文会存进本地数据库（截断到 300 字），
  用来支持「查看原文」功能。介意的话，Android 的云备份设置里可以关掉本 App 的备份。
- 不做统计、不上报、没有账号、没有广告。
